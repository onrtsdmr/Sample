<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.DashboardActivity">

    <FrameLayout
        android:id="@+id/mainContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="@drawable/layer_bottom_bar"
        app:itemBackground="@color/colorPrimary"
        app:itemIconTint="@color/selector_color_bottom_bar_item"
        app:itemTextAppearanceActive="@style/Widget.MaterialComponents.BottomNavigationView"
        app:itemTextAppearanceInactive="@style/Widget.MaterialComponents.BottomNavigationView.Colored"
        app:itemTextColor="@color/selector_color_bottom_bar_item"
        app:labelVisibilityMode="labeled"
        app:menu="@menu/menu_bottom_bar" />

</RelativeLayout>